{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for Copy OS.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "clipboard-manager:default",
    "dialog:default",
    "fs:default",
    "global-shortcut:default",
    "log:default",
    "opener:default"
  ]
}
